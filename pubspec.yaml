name: depitun
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.4+9

environment:
  sdk: ^3.10.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  shared_preferences: ^2.5.5
  google_fonts: ^8.1.0
  intl: any
  flutter_localizations:
    sdk: flutter
  image_picker: ^1.2.2
  path_provider: ^2.1.6
  uuid: ^4.5.3
  path: ^1.9.1
  confetti: ^0.8.0
  flutter_animate: ^4.5.2
  package_info_plus: ^10.2.0
  url_launcher: ^6.3.2
  share_plus: ^13.2.0
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.0
  flutter_timezone: ^5.1.0
  home_widget: ^0.9.3
  google_mobile_ads: ^9.0.0
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

# Launcher icon generation. Sources are 1024x1024 PNGs; run
# `dart run flutter_launcher_icons` to regenerate the native icons after
# changing one.
flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  # iOS + legacy Android: artwork already flattened onto the dark background
  # (iOS forbids alpha). iOS masks with a squircle, which only shaves the
  # corners, so the artwork needs no shrinking here.
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#14110D"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  # Android 8+ shows only the central 72dp of the 108dp layer, and a circular
  # mask cuts that down to a circle. The source artwork reaches 0.91 of the
  # half-canvas radius, well past the 0.667 the circle leaves visible, so it
  # has to be inset: 16% shrinks the drawable to 0.68 and lands the far tip at
  # 0.62 — clear of the circle and level with the 66dp safe zone.
  adaptive_icon_foreground_inset: 16
  remove_alpha_ios: true
  background_color_ios: "#14110D"

# Native launch screen. Deliberately a bare background with no artwork: the
# OS screen exists only to hand off invisibly to the animated Flutter splash,
# so the app appears to start *on* the splash instead of flashing a launcher
# icon first. Colour matches AppColors.charcoalDeep, which the Flutter splash
# paints under its gradient on the first frame.
#
# Android 12+ always draws an icon on its system splash and only accepts a
# solid colour (no gradient/image), so the icon is replaced with a fully
# transparent PNG to hide it. Re-run after changes:
#   dart run flutter_native_splash:create
flutter_native_splash:
  color: "#14110D"
  color_dark: "#14110D"
  android: true
  ios: true
  web: false
  # The iOS storyboard always keeps a centred LaunchImage view; left unset the
  # generator fills it with an opaque black pixel, so point it at the same
  # transparent source. It must stay large enough that the per-scale downscale
  # never rounds to a 0x0 (unreadable) PNG.
  image_ios: assets/splash/transparent.png
  image_dark_ios: assets/splash/transparent.png
  android_12:
    color: "#14110D"
    color_dark: "#14110D"
    image: assets/splash/transparent.png
    image_dark: assets/splash/transparent.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/quotes/
    - assets/lottie/
    - assets/images/
    - assets/images/textures/
    - assets/svg/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
